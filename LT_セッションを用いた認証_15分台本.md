# LT台本：ステートレスなWebで、どうログイン状態を維持するのか

## LT概要

- **テーマ**：セッションを用いた認証
- **対象者**：IT企業・SIer・コンサルティング企業などで上流工程の知識はあるが、Webの実装経験は多くない人
- **想定時間**：12〜13分（上限15分）
- **このLTのゴール**：Cookie、session_id、Session Storeが連携して、ユーザーを識別する仕組みを説明できるようになる
- **中心メッセージ**：セッション認証とは、クライアントが送るsession_idを鍵にして、サーバー側の認証状態を参照する仕組みである

---

## 時間配分

| 時間 | 内容 |
|---:|---|
| 0:00〜0:45 | 導入：一度ログインすると、なぜログイン状態が続くのか |
| 0:45〜2:15 | HTTPのRequest／Responseとステートレス |
| 2:15〜3:15 | RESTにおけるステートレス |
| 3:15〜4:15 | 問い：同じユーザーだと、どう判断するのか |
| 4:15〜7:00 | ログイン時の処理 |
| 7:00〜9:30 | ログイン後の処理 |
| 9:30〜10:30 | CookieとSessionの違い |
| 10:30〜12:00 | 複数サーバー構成とSession Store |
| 12:00〜13:00 | まとめ |
| 13:00〜15:00 | 説明超過の吸収・質疑への移行 |

---

## 1. 導入：一度ログインすると、なぜログイン状態が続くのか

### スライドに載せる内容

**一度ログインしただけで、その後も「自分」だと分かるのはなぜ？**

```text
① ログインする
② マイページを見る
③ 注文履歴を見る
④ 設定を変更する
```

### 話す内容（約45秒）

今日は「セッションを用いた認証」についてお話しします。

Webサービスでは、一度ログインすると、その後マイページや注文履歴を開いても、毎回IDとパスワードを入力する必要はありません。

ユーザーからすると当たり前の動きですが、Webの通信は基本的に、前の通信をそのまま覚えているわけではありません。

では、サーバーはどうやって「このアクセスは、さっきログインした人から来たものだ」と判断しているのでしょうか。今日は、その仕組みをセッション認証を題材に見ていきます。

### このパートで伝えること

- 身近な疑問を提示する
- 今日の話を「認証方式の用語解説」ではなく「疑問を解く話」にする

---

## 2. HTTPのRequest／Responseとステートレス

### スライドに載せる内容

```text
ブラウザ                    サーバー
   |                           |
   |--- GET /mypage ---------> |
   |                           |
   |<------ 200 OK ----------- |
```

- クライアントがRequestを送る
- サーバーがResponseを返す
- 1回のRequest／Responseで通信が完結する

### 話す内容（約1分30秒）

まず、前提となるHTTPについて、今回必要な部分だけ確認します。

ブラウザなどのクライアントが、サーバーにRequestを送ります。例えば、マイページを取得するために `GET /mypage` を送ります。サーバーは処理を行い、Responseを返します。

基本的には、この1回のRequestとResponseで一つの通信が完結します。

次に別のRequestが来たとき、HTTPの通信そのものには、「前回のRequestを送った人と同じ人です」という情報が自動的に引き継がれるわけではありません。

このように、個々の通信が独立している性質を、ここでは「ステートレス」と捉えてください。

### 補足として言わないこと

- HTTPメソッドの網羅的な説明
- HTTPヘッダーの細かな仕様
- TCPやTLSの説明

---

## 3. RESTにおけるステートレス

### スライドに載せる内容

> 各Requestは、それ単体で処理に必要な情報を持つ

```text
Request 1：POST /login
Request 2：GET  /products
Request 3：GET  /mypage
```

**ステートレス ≠ サーバーが一切データを持たない**

### 話す内容（約1分）

RESTにはいくつかの原則がありますが、今日はステートレスという特徴だけを取り上げます。

RESTにおけるステートレスとは、各Requestが、それ単体で処理するために必要な情報を持つという考え方です。

ここで注意したいのは、「ステートレスだから、サーバーはデータや状態を一切持ってはいけない」という意味ではないことです。

DBにユーザー情報を保存することもできますし、これから説明するセッション情報をサーバー側で管理することもできます。重要なのは、Requestを処理するための手掛かりが、Requestごとに渡されることです。

---

## 4. 問い：同じユーザーだと、どう判断するのか

### スライドに載せる内容

```text
POST /login

        ↓ 別の独立した通信

GET /mypage
```

**サーバーは、なぜ同じユーザーだと分かる？**

### 話す内容（約1分）

ここで最初の疑問に戻ります。

ユーザーが `POST /login` でログインしたあと、`GET /mypage` を送ったとします。この二つは、HTTP上では別々のRequestです。

そのままでは、サーバーはマイページへのアクセスが、先ほどログインしたユーザーから来たものなのか判断できません。

そこで使われるのが、CookieとSessionです。ここからは、ログイン時とログイン後の二つに分けて見ていきます。

---

## 5. ログイン時の処理

### スライドに載せる内容

```text
ブラウザ                    サーバー                 Session Store
   |                           |                           |
   |-- ID・Password ---------> |                           |
   |                           |-- ID・Passwordを確認      |
   |                           |-- session_idを発行 ------>|
   |                           |   abc123 → user_id: 100   |
   |<- Set-Cookie:             |                           |
   |   session_id=abc123 ------|                           |
```

### 話す内容（約2分45秒）

まずはログイン時の処理です。

1つ目に、ブラウザからサーバーへ、ユーザーIDとパスワードを送ります。

2つ目に、サーバーは、送られた情報が正しいかを確認します。ここで本人確認に成功すると、サーバーはランダムで推測困難な `session_id` を発行します。ここでは `abc123` とします。

3つ目に、サーバー側のSession Storeへ、「session_idがabc123なら、user_idは100」という対応関係を保存します。Session Storeには、DBやRedisなどが使われます。

4つ目に、サーバーはResponseの `Set-Cookie` ヘッダーを使って、`session_id=abc123` をブラウザへ返します。ブラウザは、受け取ったCookieを保存します。

この時点で、ブラウザ側にあるのは、基本的にはユーザーのパスワードそのものではなく、サーバー側のセッション情報を参照するための鍵であるsession_idです。

なお、実際にはCookieに `HttpOnly`、`Secure`、`SameSite` などの属性を設定して安全性を高めますが、今日は全体の仕組みに焦点を当てるため、詳細は割愛します。

### 発表時のポイント

- 図の矢印を上から順に指しながら説明する
- `abc123 → user_id: 100` の対応関係を強調する
- 「パスワードをCookieへ保存する仕組みではない」と補足する

---

## 6. ログイン後の処理

### スライドに載せる内容

```text
ブラウザ                    サーバー                 Session Store
   |                           |                           |
   |-- GET /mypage ----------> |                           |
   |   Cookie:                 |                           |
   |   session_id=abc123       |                           |
   |                           |-- abc123を検索 ---------->|
   |                           |<- user_id: 100 -----------|
   |                           |                           |
   |<- user_id: 100の情報 -----|                           |
```

### 話す内容（約2分30秒）

次に、ログイン後にマイページへアクセスする場合です。

ブラウザは、保存したCookieを、対象のサーバーへの次のRequestにも自動的に付けて送ります。つまり、`GET /mypage` と一緒に `session_id=abc123` がサーバーへ届きます。

サーバーは、受け取ったsession_idを使ってSession Storeを検索します。すると、「abc123に対応するのはuser_id 100」という情報が見つかります。

これによりサーバーは、今回のRequestを送ったユーザーをuser_id 100として扱えます。最後に、そのユーザーのマイページ情報を取得してResponseを返します。

ログイン後のRequestでも、処理に必要なsession_idが毎回Cookieとして送られています。そのため、ログイン状態を扱いながらも、各Request単位で処理に必要な情報を受け取る形になっています。

実装では、認証が必要な複数のAPIに対し、この確認処理を共通のミドルウェアとして配置することがよくあります。

### 擬似コードを載せる場合

```go
sessionID := getCookie(request)
session := sessionStore.Find(sessionID)
user := users.Find(session.UserID)
return user
```

※時間が厳しい場合、擬似コードはスライドに載せても読み上げない。

---

## 7. CookieとSessionの違い

### スライドに載せる内容

| 要素 | 役割 | 主な保存場所 |
|---|---|---|
| Cookie | session_idをRequestに載せて運ぶ | クライアント側 |
| Session | ログイン中のユーザー情報を管理する | サーバー側 |
| session_id | CookieとSessionを結び付ける鍵 | 両者の橋渡し |

**Cookieは鍵、Session Storeは鍵に対応する情報の保管場所**

### 話す内容（約1分）

ここで、混同しやすいCookieとSessionを整理します。

Cookieは、ブラウザ側に保存され、session_idをRequestに載せて運ぶ役割を持ちます。

Sessionはサーバー側で管理され、session_idとユーザーを対応付けます。そしてsession_idが、両者を結び付ける鍵になります。

つまり、「CookieそのものがSession」なのではありません。Cookieが持つ鍵を使って、サーバー側のSessionを参照する関係です。

---

## 8. 複数サーバー構成とSession Store

### スライドに載せる内容

```text
                    ┌─ Server A ─┐
ブラウザ ── LB ─────┼─ Server B ─┼── Redisなどの共有Session Store
                    └─ Server C ─┘
```

- 各サーバーのメモリだけにSessionを保存すると共有できない
- 共有Session Storeを使えば、どのサーバーでも認証できる
- 認証方式はインフラ構成にも影響する

### 話す内容（約1分30秒）

最後に、システム構成との関係を見ます。

サーバーが1台だけなら、そのサーバーのメモリにSessionを保存する構成も考えられます。

しかしサーバーが複数台あると、ログイン処理をServer Aが受け、その次のRequestをServer Bが受けることがあります。SessionがServer Aのメモリにしかなければ、Server Bはそのユーザーを認証できません。

そこで、Redisなどの共有Session Storeを用意し、すべてのサーバーが同じセッション情報を参照できるようにします。

このように、認証方式の選択はアプリケーション内部だけの話ではなく、可用性やスケーリングを含むシステム構成にも影響します。

### 時間が足りない場合

このパートを次の一言に短縮する。

> サーバーを複数台にする場合は、どのサーバーでも同じSessionを参照できるよう、Redisなどの共有Session Storeを使う構成があります。

---

## 9. まとめ

### スライドに載せる内容

1. HTTPのRequestは、それぞれ独立している
2. ログイン時、サーバーはSessionを作り、session_idをCookieで返す
3. ログイン後、ブラウザはsession_idを毎回送る
4. サーバーはsession_idからユーザーを識別する

> セッション認証とは、クライアントが送るsession_idを鍵にして、サーバー側の認証状態を参照する仕組み

### 話す内容（約1分）

まとめです。

HTTPのRequestは、それぞれ独立しています。そのままでは、サーバーは別々のRequestを送った人が同じユーザーだと判断できません。

そこでログイン時に、サーバー側へSessionを作り、そのSessionを参照するためのsession_idをCookieでブラウザへ渡します。

ログイン後は、ブラウザがsession_idをRequestごとに送り、サーバーがSession Storeを参照することでユーザーを識別します。

つまりセッション認証とは、クライアントが送るsession_idを鍵にして、サーバー側の認証状態を参照する仕組みです。

以上で発表を終わります。

---

## 発表者用メモ

### 15分を超えないためのルール

- HTTPは「Request／Response」「通信が独立」の2点だけ説明する
- RESTの原則を列挙しない
- Cookieのセキュリティ属性は名前の紹介だけにする
- JWTとの比較は本編に入れず、質問された場合に答える
- 擬似コードは読み上げない
- 12分時点で複数サーバー構成に入っていなければ、そのパートを短縮する

### 想定質問

#### Q1. ステートレスなのに、サーバー側にSessionを持ってよいのですか？

はい。RESTのステートレスは、サーバーが一切データを持たないという意味ではありません。各Requestが処理に必要な情報を持つことが重要です。セッション認証では、Requestごとにsession_idが送られ、サーバーはそれを手掛かりにSessionを参照します。

#### Q2. CookieとSessionは同じものですか？

異なります。Cookieはブラウザ側に保存され、session_idをサーバーへ運びます。Sessionはサーバー側で管理され、session_idとユーザーを対応付けます。

#### Q3. session_idを盗まれたらどうなりますか？

攻撃者が本人としてアクセスできる可能性があります。そのため、HTTPSの利用、推測困難なsession_id、Cookieの `HttpOnly`・`Secure`・`SameSite` 属性、有効期限、ログイン後のsession_id再発行などが重要です。

#### Q4. ログアウトでは何をしますか？

サーバー側のSessionを無効化または削除し、ブラウザ側のCookieも期限切れにします。Cookieだけを消すのではなく、サーバー側でも使えない状態にすることが重要です。

#### Q5. SessionとJWTはどちらが優れていますか？

一律には決まりません。Sessionはサーバー側で無効化しやすい一方、共有Session Storeの設計が必要です。JWTは各サーバーで検証しやすい一方、発行済みTokenの即時無効化には工夫が必要です。要件に応じて選択します。

---

## スライド枚数の目安

| No. | タイトル |
|---:|---|
| 1 | ステートレスなWebで、どうログイン状態を維持するのか |
| 2 | HTTPのRequest／Response |
| 3 | RESTにおけるステートレス |
| 4 | サーバーは、なぜ同じユーザーだと分かる？ |
| 5 | セッション認証：ログイン時 |
| 6 | セッション認証：ログイン後 |
| 7 | CookieとSessionの違い |
| 8 | 複数サーバーとSession Store |
| 9 | まとめ |

